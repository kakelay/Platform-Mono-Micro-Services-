spring:
  profiles: prod
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/platform_user_db
    username: ${DB_USERNAME:platform_user_user}
    password: ${DB_PASSWORD:platform_user_password}
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:prod-jwt-secret}
