spring:
  profiles: uat
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/platform_payment_db
    username: ${DB_USERNAME:platform_payment_user}
    password: ${DB_PASSWORD:platform_payment_password}
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:uat-jwt-secret}
