spring:
  datasource:
    url: jdbc:h2:mem:customers;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
security:
  jwt:
    secret: test-jwt-secret-test-jwt-secret
eureka:
  client:
    enabled: false
