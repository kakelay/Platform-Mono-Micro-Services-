spring:
  profiles: prod
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/platform_customer_db
    username: ${DB_USERNAME:platform_customer_user}
    password: ${DB_PASSWORD:platform_customer_password}
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:prod-jwt-secret}
