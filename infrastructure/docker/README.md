# Docker Infrastructure

Use this directory for shared Docker build resources and reusable container patterns.

The service-specific Dockerfiles will be added to each microservice folder.

For infrastructure support, read the `deployment/docker-compose/` manifests when they are created.
