spring:
  profiles: prod
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/platform_notification_db
    username: ${DB_USERNAME:platform_notification_user}
    password: ${DB_PASSWORD:platform_notification_password}
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:prod-jwt-secret}
