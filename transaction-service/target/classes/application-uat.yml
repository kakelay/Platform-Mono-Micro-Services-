spring:
  profiles: uat
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/platform_transaction_db
    username: ${DB_USERNAME:platform_transaction_user}
    password: ${DB_PASSWORD:platform_transaction_password}
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:uat-jwt-secret}
