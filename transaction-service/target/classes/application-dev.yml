spring:
  profiles: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/platform_transaction_db
    username: platform_transaction_user
    password: platform_transaction_password
security:
  jwt:
    secret: dev-jwt-secret
