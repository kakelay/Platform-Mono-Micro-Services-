spring:
  profiles: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/platform_audit_db
    username: platform_audit_user
    password: platform_audit_password
security:
  jwt:
    secret: dev-jwt-secret
