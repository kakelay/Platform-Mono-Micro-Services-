spring:
  profiles: uat
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/platform_audit_db
    username: ${DB_USERNAME:platform_audit_user}
    password: ${DB_PASSWORD:platform_audit_password}
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:uat-jwt-secret}
