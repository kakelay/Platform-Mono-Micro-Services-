spring:
  profiles: uat
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/platform_order_db
    username: ${DB_USERNAME:platform_order_user}
    password: ${DB_PASSWORD:platform_order_password}
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:uat-jwt-secret}
