server:
  port: 8081
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/platform_auth_db
    username: ${DB_USERNAME:platform_auth_user}
    password: ${DB_PASSWORD:platform_auth_password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
spring:
  security:
    user:
      name: none
      password: none
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
  endpoint:
    health:
      show-details: always
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:default-jwt-secret-key-default-jwt-secret-key}
    access-token-expire-seconds: 900
    refresh-token-expire-seconds: 2592000
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://discovery-service:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
