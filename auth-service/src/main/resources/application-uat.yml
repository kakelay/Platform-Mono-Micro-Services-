spring:
  profiles: uat
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/platform_auth_db
    username: ${DB_USERNAME:platform_auth_user}
    password: ${DB_PASSWORD:platform_auth_password}
security:
  jwt:
    secret: ${SECURITY_JWT_SECRET:uat-secret-key-uat-secret-key}
