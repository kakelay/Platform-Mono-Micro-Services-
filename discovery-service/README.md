# Discovery Service

`discovery-service` provides service registration and discovery using Eureka.

## Features
- Registers microservices for runtime discovery
- Exposes Eureka dashboard on port 8761
- Supports environment-specific configuration via profiles

## Notes
For Kubernetes production deployments, use native Kubernetes service discovery rather than Eureka.
